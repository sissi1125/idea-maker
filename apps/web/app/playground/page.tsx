/**
 * Playground page — 保留旧 RAG Playground 入口
 * 从 feat-100.x 迁来，供高级调试使用。
 */

import PlaygroundShell from "@/components/playground/PlaygroundShell";

export default function PlaygroundPage() {
  return <PlaygroundShell />;
}
